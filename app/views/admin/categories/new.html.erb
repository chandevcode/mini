<% content_for :title, "New Category" %>

<div class="md:w-2/3 container mx-auto px-4 py-8">
  <h1 class="font-bold text-4xl">New Category</h1>

  <%= render "form", category: @category %>

  <%= link_to "Back to Admin", admin_categories_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
